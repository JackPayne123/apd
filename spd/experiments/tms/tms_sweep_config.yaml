program: spd/experiments/tms/tms_decomposition.py
method: grid
metric:
  name: final_closeness
  goal: minimize
parameters:
  # seed:
  #   values: [0, 1]
  topk_l2_coeff:
    # values: [1e1, 1e-1, 0]
    values: [1, 1e-1]
  topk_recon_coeff:
    values: [1e1, 1]
  # topk_recon_coeff:
  #   values: [0]
  topk_act_recon_coeff:
    # values: [1e-2, 0]
    values: [1e-2, 0]
  attribution_type:
    values: [activation]
command:
- ${env}
- ${interpretere
- ${program}
- spd/experiments/tms/tms_topk_full_rank_config.yaml