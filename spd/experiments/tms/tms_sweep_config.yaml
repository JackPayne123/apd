program: spd/experiments/tms/tms_decomposition.py
method: grid
metric:
  name: final_closeness
  goal: minimize
parameters:
  lr:
    values: [1e-3, 3e-4]
  lp_sparsity_coeff:
    values: [10, 1, 0.1, 0.01]
command:
- ${env}
- ${interpreter}
- ${program}
- spd/experiments/tms/tms_lp_config.yaml