program: spd/experiments/linear/linear_decomposition.py
method: grid
metric:
  name: final_closeness
  goal: minimize
parameters:
  ablation_attributions:
    values: [true, false]
  topk_l2_coeff:
    values: [1, 6]

command:
- ${env}
- ${interpreter}
- ${program}
- spd/experiments/linear/linear_topk_full_rank_config.yaml
